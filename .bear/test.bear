# Bear sample script.
# this enables the session-scope, task is distributed between hosts
#:set stage='two'
#:set groovyShell.sendToHosts=true

:use shell ssh {"name": "shell ex. 1"}
pwd
echo `hostname`

:use shell ssh {"name": "shell ex. 2"}
cat /home/andrey/texty

:use shell groovy {"name": "groovy ex. 1"}
:set groovyShell.sendToHosts=true
:set bear.interactiveRun=false
:set bear.autoInstallPlugins=true
#runner.run(tasks.setup)